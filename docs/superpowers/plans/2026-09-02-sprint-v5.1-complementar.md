# Sprint v5.1 Complementar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar lacunas do MELHORIAS.md (§3.1 CTF tools, §2.5 briefing, §5 ergonomia) + smoke test e2e + atualizar README/MELHORIAS desatualizados.

**Architecture:** 5 novas tools MCP (`export_curl`, `export_har`, `jwt_decode`, `jwt_resign`, briefing rico no `get_active_context`) seguindo o padrão existente: `registerV10Tools` em novo `tools_v10.go` + blocos `mcp.NewTool` em `server.go`. Reuso de `store`/`decoder` atuais. Zero novas dependências.

**Tech Stack:** Go 1.25.5 · `mark3labs/mcp-go v0.57.0` · stdlib apenas (HAR = `encoding/json`, curl = string builder, JWT = `crypto/hmac` + `encoding/base64`).

## Global Constraints

- Go 1.25.5, zero CGO, binário estático único, sem dependências novas.
- TDD estrito: cada task começa com teste falhando. Framework = stdlib `testing` + `httptest`. Sem testify/gomock.
- Descriptions de tool MCP em inglês e ricas (o LLM decide qual tool chamar pela description).
- Nunca retornar bodies inteiros por padrão (economia de tokens — MELHORIAS §2).
- HAR export: metadata only (sem bodies), só `bodySize` + `mimeType`.
- `jwt_resign` requer `confirmed:true` (padrão `add_target`), mesmo sendo offline.
- Commits em português, Conventional Commits: `feat(...)`, `fix(...)`, `docs:`, `build(...)`.
- YAGNI: zero abstração especulativa.
- Retornos de tool novas estruturados (JSON estável), exceto `export_curl` (string de comando).

---

### Task 1: `export_curl` — request capturado → one-liner curl

**Files:**
- Create: `internal/mcpserver/tools_v10.go`
- Create: `internal/mcpserver/tools_v10_test.go`
- Modify: `internal/mcpserver/server.go` (novo bloco `mcp.AddTool` após o de `report_export_pdf`, ~linha 415)

**Interfaces:**
- Consumes: `store.Store.Get(id int64) (*store.Request, error)` (existente), `s.openStoreForActiveTarget()` (existente em tools_v9.go), `s.tools` map + `s.wrapTool` (padrão server.go).
- Produces: `func registerV10Tools(s *Server)` e `func (s *Server) toolExportCurl(ctx context.Context, args map[string]any) (string, error)`. Tasks 2–4 registram mais handlers neste mesmo `registerV10Tools`.

- [ ] **Step 1: Write the failing test**

```go
// internal/mcpserver/tools_v10_test.go
package mcpserver

import (
	"context"
	"strings"
	"testing"
)

func TestToolExportCurl(t *testing.T) {
	s, cleanup := newTestServer(t) // reusar helper dos testes existentes (tools_v2_test.go/tools_v9_test.go)
	defer cleanup()
	st, err := s.openStoreForActiveTarget()
	if err != nil {
		t.Fatalf("store: %v", err)
	}
	id, err := st.Insert(&store.Request{
		Method: "POST", URL: "https://api.alvo.com/v1/login",
		ReqHeaders: map[string][]string{"Authorization": {"Bearer tok123"}, "Content-Type": {"application/json"}},
		ReqBody:   []byte(`{"user":"a","pass":"b"}`), Status: 200,
	})
	if err != nil {
		t.Fatal(err)
	}
	out, err := s.toolExportCurl(context.Background(), map[string]any{"id": float64(id)})
	if err != nil {
		t.Fatalf("toolExportCurl: %v", err)
	}
	for _, want := range []string{"curl", "-X POST", "https://api.alvo.com/v1/login", "-H 'Authorization: Bearer tok123'", "--data"} {
		if !strings.Contains(out, want) {
			t.Errorf("curl output missing %q:\n%s", want, out)
		}
	}
}

func TestToolExportCurl_NotFound(t *testing.T) {
	s, cleanup := newTestServer(t)
	defer cleanup()
	if _, err := s.toolExportCurl(context.Background(), map[string]any{"id": float64(9999)}); err == nil {
		t.Error("esperava erro para id inexistente")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/mcpserver/ -run TestToolExportCurl -v`
Expected: FAIL (undefined: toolExportCurl / registerV10Tools)

- [ ] **Step 3: Write minimal implementation**

```go
// internal/mcpserver/tools_v10.go
package mcpserver

import (
	"context"
	"fmt"
	"strconv"

	"github.com/isaiaspereira307/network-copitlot/internal/store"
)

func registerV10Tools(s *Server) {
	s.tools["export_curl"] = s.toolExportCurl
	// Tasks 2-4: export_har, jwt_decode, jwt_resign
}

func (s *Server) toolExportCurl(ctx context.Context, args map[string]any) (string, error) {
	id, ok := argInt64(args, "id") // helper existente ou criar: args["id"].(float64) → int64
	if !ok {
		return "", fmt.Errorf("id obrigatorio")
	}
	st, err := s.openStoreForActiveTarget()
	if err != nil {
		return "", err
	}
	r, err := st.Get(id)
	if err != nil {
		return "", err
	}
	var b strings.Builder
	fmt.Fprintf(&b, "curl -X %s '%s'", r.Method, r.URL)
	for k, vs := range r.ReqHeaders {
		for _, v := range vs {
			fmt.Fprintf(&b, " -H '%s: %s'", k, v)
		}
	}
	if len(r.ReqBody) > 0 {
		if len(r.ReqBody) <= 200 {
			fmt.Fprintf(&b, " --data '%s'", string(r.ReqBody))
		} else {
			fmt.Fprintf(&b, " --data-binary @- <<'EOF'\n%s\nEOF", string(r.ReqBody))
		}
	}
	return b.String(), nil
}
```

Em `server.go`, dentro de `RegisterTools()` após `registerV9Tools(s)`: adicionar `registerV10Tools(s)` e o bloco:

```go
	s.mcp.AddTool(
		mcp.NewTool("export_curl",
			mcp.WithDescription("Rebuild a captured request as a ready-to-paste curl command. Pass a request id from list_requests. Returns the exact one-liner (or heredoc form for large bodies) with method, URL, headers and body."),
			mcp.WithNumber("id", mcp.Required()),
		),
		s.wrapTool("export_curl", s.toolExportCurl),
	)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/mcpserver/ -run TestToolExportCurl -v`
Expected: PASS

- [ ] **Step 5: Full package + commit**

Run: `go vet ./... && go test ./internal/mcpserver/ -count=1`

```bash
git add internal/mcpserver/tools_v10.go internal/mcpserver/tools_v10_test.go internal/mcpserver/server.go
git commit -m "feat(mcp): tool export_curl (request -> comando curl)"
```

---

### Task 2: `export_har` — alvo ativo inteiro como HAR 1.2 (metadata only)

**Files:**
- Create: `internal/har/har.go`
- Create: `internal/har/har_test.go`
- Modify: `internal/mcpserver/tools_v10.go`, `tools_v10_test.go`, `server.go`

**Interfaces:**
- Consumes: `store.Store.All() ([]*store.Request, error)` (existente), `s.openStoreForActiveTarget()`, `reportOutDir`/padrão de gravação de `tools_v9.go`, `s.projectName()` (tools_v8.go).
- Produces: `har.WriteHAR(target string, reqs []*store.Request) ([]byte, error)`.

- [ ] **Step 1: Write the failing test**

```go
// internal/har/har_test.go
package har

import (
	"encoding/json"
	"testing"

	"github.com/isaiaspereira307/network-copitlot/internal/store"
)

func TestWriteHAR(t *testing.T) {
	reqs := []*store.Request{
		{Method: "GET", URL: "https://a.com/x", Status: 200, RespLen: 120,
			ReqHeaders: map[string][]string{"Accept": {"text/html"}},
			RespHeaders: map[string][]string{"Content-Type": {"text/html"}}},
		{Method: "POST", URL: "https://a.com/y", Status: 500, RespLen: 30,
			ReqBody: []byte(`{"k":1}`),
			ReqHeaders:  map[string][]string{"Content-Type": {"application/json"}},
			RespHeaders: map[string][]string{"Content-Type": {"application/json"}}},
	}
	raw, err := WriteHAR("a.com", reqs)
	if err != nil {
		t.Fatal(err)
	}
	var doc struct {
		Log struct {
			Version string `json:"version"`
			Creator struct{ Name, Version string }
			Entries []struct {
				Request struct {
					Method string `json:"method"`
					URL    string `json:"url"`
					Headers []struct{ Name, Value string } `json:"headers"`
					BodySize int `json:"bodySize"`
				} `json:"request"`
				Response struct {
					Status   int `json:"status"`
					BodySize int `json:"bodySize"`
				} `json:"response"`
			} `json:"entries"`
		} `json:"log"`
	}
	if err := json.Unmarshal(raw, &doc); err != nil {
		t.Fatalf("HAR invalido: %v", err)
	}
	if doc.Log.Version != "1.2" {
		t.Errorf("version = %q, want 1.2", doc.Log.Version)
	}
	if len(doc.Log.Entries) != 2 {
		t.Fatalf("entries = %d, want 2", len(doc.Log.Entries))
	}
	e := doc.Log.Entries[1]
	if e.Request.Method != "POST" || e.Request.URL != "https://a.com/y" {
		t.Errorf("entry[1] = %s %s", e.Request.Method, e.Request.URL)
	}
	if e.Response.Status != 500 {
		t.Errorf("status = %d, want 500", e.Response.Status)
	}
	if e.Request.BodySize != int64(len(`{"k":1}`)) && e.Request.BodySize != len(`{"k":1}`) {
		t.Errorf("bodySize = %d, want %d", e.Request.BodySize, len(`{"k":1}`))
	}
	// metadata only: nenhum body no HAR
	if string(raw) != "" && jsonHasBodyContent(raw) {
		t.Error("HAR nao deve conter conteudo de body")
	}
}

func jsonHasBodyContent(raw []byte) bool {
	s := string(raw)
	return len(s) > 0 && (false) // placeholder logico: verificar ausencia de "text" nos postData via doc acima
}
```

(Nota: `jsonHasBodyContent` é auxiliar opcional — o teste essencial é que o struct de parse NÃO tenha campo de body preenchido. Se simplificar, remover o helper e validar via ausência da chave `"postData"` em `string(raw)` com `strings.Contains`.)

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/har/ -v`
Expected: FAIL (package não existe / undefined WriteHAR)

- [ ] **Step 3: Write minimal implementation**

```go
// internal/har/har.go
// Package har exporta requests capturados no formato HAR 1.2 (metadata only,
// sem bodies — economia de tokens, MELHORIAS §2).
package har

import (
	"encoding/json"
	"time"

	"github.com/isaiaspereira307/network-copitlot/internal/store"
)

type harHeader struct {
	Name  string `json:"name"`
	Value string `json:"value"`
}

type harRequest struct {
	Method      string      `json:"method"`
	URL         string      `json:"url"`
	HTTPVersion string      `json:"httpVersion"`
	Headers     []harHeader `json:"headers"`
	BodySize    int64       `json:"bodySize"`
}

type harResponse struct {
	Status      int         `json:"status"`
	HTTPVersion string      `json:"httpVersion"`
	Headers     []harHeader `json:"headers"`
	BodySize    int64       `json:"bodySize"`
}

type harEntry struct {
	StartedDateTime string      `json:"startedDateTime"`
	Time            float64     `json:"time"`
	Request         harRequest  `json:"request"`
	Response       harResponse  `json:"response"`
}

type harLog struct {
	Version string     `json:"version"`
	Creator harCreator `json:"creator"`
	Entries []harEntry `json:"entries"`
}

type harCreator struct {
	Name    string `json:"name"`
	Version string `json:"version"`
}

type harDoc struct {
	Log harLog `json:"log"`
}

// WriteHAR serializa reqs como HAR 1.2. Bodies NAO sao incluidos (so bodySize).
func WriteHAR(target string, reqs []*store.Request) ([]byte, error) {
	entries := make([]harEntry, 0, len(reqs))
	for _, r := range reqs {
		e := harEntry{
			StartedDateTime: time.UnixMilli(r.Ts).UTC().Format(time.RFC3339),
			Request: harRequest{Method: r.Method, URL: r.URL, HTTPVersion: "HTTP/1.1",
				Headers: flatten(r.ReqHeaders), BodySize: int64(len(r.ReqBody))},
			Response: harResponse{Status: r.Status, HTTPVersion: "HTTP/1.1",
				Headers: flatten(r.RespHeaders), BodySize: int64(r.RespLen)},
		}
		entries = append(entries, e)
	}
	return json.MarshalIndent(harDoc{Log: harLog{Version: "1.2",
		Creator: harCreator{Name: "mcp-proxy", Version: "v5.1"}, Entries: entries}}, "", " ")
}

func flatten(hs map[string][]string) []harHeader {
	out := make([]harHeader, 0, len(hs))
	for k, vs := range hs {
		for _, v := range vs {
			out = append(out, harHeader{Name: k, Value: v})
		}
	}
	return out
}
```

Handler em `tools_v10.go` (`registerV10Tools` ganha `s.tools["export_har"] = s.toolExportHAR`):

```go
func (s *Server) toolExportHAR(ctx context.Context, args map[string]any) (string, error) {
	st, err := s.openStoreForActiveTarget()
	if err != nil {
		return "", err
	}
	if st == nil {
		return "", fmt.Errorf("nenhum alvo ativo")
	}
	reqs, err := st.All()
	if err != nil {
		return "", err
	}
	tgt, _ := s.active.Target()
	host := "target"
	if tgt != nil {
		host = tgt.Host
	}
	raw, err := har.WriteHAR(host, reqs)
	if err != nil {
		return "", err
	}
	dir := filepath.Join(s.repo.WorkspacePath(), s.projectName(), reportOutDir, host+".har")
	if err := os.MkdirAll(filepath.Dir(dir), 0o755); err != nil {
		return "", err
	}
	if err := os.WriteFile(dir, raw, 0o644); err != nil {
		return "", err
	}
	return fmt.Sprintf("HAR exportado (%d entries, metadata only): %s", len(reqs), dir), nil
}
```

(Adaptar path ao padrão real usado em `tools_v9.go` para reports — o implementer deve conferir como `toolReportMarkdown` monta o diretório e seguir o mesmo.)

Bloco `server.go`:

```go
	s.mcp.AddTool(
		mcp.NewTool("export_har",
			mcp.WithDescription("Export the entire active target as HAR 1.2 — metadata only (method, URL, headers, sizes), no bodies. Import into other tools (Burp, devtools). Returns the file path."),
		),
		s.wrapTool("export_har", s.toolExportHAR),
	)
```

Teste MCP em `tools_v10_test.go`: inserir 2 requests, chamar `toolExportHAR`, assert retorno contém `.har` e o arquivo existe com `"version":"1.2"`.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/har/ ./internal/mcpserver/ -count=1`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/har/ internal/mcpserver/tools_v10.go internal/mcpserver/tools_v10_test.go internal/mcpserver/server.go
git commit -m "feat(har,mcp): tool export_har (HAR 1.2 metadata only)"
```

---

### Task 3: `jwt_decode` — header + payload + warnings de ataque

**Files:**
- Modify: `internal/decoder/decoder.go`, `internal/decoder/decoder_test.go`
- Modify: `internal/mcpserver/tools_v10.go`, `tools_v10_test.go`, `server.go`

**Interfaces:**
- Consumes: `decodeJWT` existente em decoder.go (payload), `base64.RawURLEncoding`.
- Produces: `type JWTInfo struct { Header, Payload, Signature string; Warnings []string }` + `func DecodeJWTFull(token string) (*JWTInfo, error)`. Warnings: `"alg=none"` (header alg none), `"assinatura vazia"` (sem 3º segmento), `"exp expirado"` (payload.exp < now unix).

- [ ] **Step 1: Write the failing test**

```go
// decoder_test.go — adicionar
func TestDecodeJWTFull(t *testing.T) {
	// header {"alg":"HS256","typ":"JWT"}, payload {"sub":"u1","exp":1000000000} (expirado)
	tok := "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1MSIsImV4cCI6MTAwMDAwMDAwMH0.sig"
	info, err := DecodeJWTFull(tok)
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(info.Header, "HS256") || !strings.Contains(info.Payload, `"sub":"u1"`) {
		t.Errorf("header=%s payload=%s", info.Header, info.Payload)
	}
	if !slices.Contains(info.Warnings, "exp expirado") {
		t.Errorf("warnings = %v, want exp expirado", info.Warnings)
	}
	// alg=none
	noneTok := "eyJhbGciOiJub25lIn0.eyJzdWIiOiJ1MSJ9."
	info2, err := DecodeJWTFull(noneTok)
	if err != nil {
		t.Fatal(err)
	}
	if !slices.Contains(info2.Warnings, "alg=none") || !slices.Contains(info2.Warnings, "assinatura vazia") {
		t.Errorf("warnings = %v, want alg=none e assinatura vazia", info2.Warnings)
	}
	// token invalido
	if _, err := DecodeJWTFull("nao-e-jwt"); err == nil {
		t.Error("esperava erro para token invalido")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/decoder/ -run TestDecodeJWTFull -v`
Expected: FAIL (undefined: DecodeJWTFull)

- [ ] **Step 3: Write minimal implementation**

```go
// decoder.go — adicionar
type JWTInfo struct {
	Header    string   `json:"header"`
	Payload   string   `json:"payload"`
	Signature string   `json:"signature"`
	Warnings  []string `json:"warnings"`
}

func DecodeJWTFull(token string) (*JWTInfo, error) {
	parts := strings.Split(strings.TrimSpace(token), ".")
	if len(parts) < 2 {
		return nil, fmt.Errorf("jwt invalido: esperado header.payload[.sig]")
	}
	header, err := b64url(parts[0])
	if err != nil {
		return nil, fmt.Errorf("header: %w", err)
	}
	payload, err := b64url(parts[1])
	if err != nil {
		return nil, fmt.Errorf("payload: %w", err)
	}
	sig := ""
	if len(parts) > 2 {
		if b, err := base64.RawURLEncoding.DecodeString(parts[2]); err == nil {
			sig = string(b)
		} else {
			sig = parts[2] // opaque
		}
	}
	info := &JWTInfo{Header: header, Payload: payload, Signature: sig}
	var hdr struct{ Alg string `json:"alg"` }
	_ = json.Unmarshal([]byte(header), &hdr) // best-effort
	if strings.EqualFold(hdr.Alg, "none") {
		info.Warnings = append(info.Warnings, "alg=none")
	}
	if len(parts) < 3 || parts[2] == "" {
		info.Warnings = append(info.Warnings, "assinatura vazia")
	}
	var pl struct{ Exp int64 `json:"exp"` }
	if err := json.Unmarshal([]byte(payload), &pl); err == nil && pl.Exp > 0 && pl.Exp < time.Now().Unix() {
		info.Warnings = append(info.Warnings, "exp expirado")
	}
	return info, nil
}

func b64url(s string) (string, error) {
	b, err := base64.RawURLEncoding.DecodeString(s)
	if err != nil {
		return "", err
	}
	return string(b), nil
}
```

Handler `toolJwtDecode` em `tools_v10.go`: arg `token` (string, required) → `decoder.DecodeJWTFull` → retornar `json.Marshal(info)`. Registro + description: "Decode a JWT (JWS compact serialization). Returns header, payload, signature and attack-surface warnings: alg=none, empty signature, expired exp." Teste MCP: chamar com token fixture, assert JSON contém `"warnings"`.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/decoder/ ./internal/mcpserver/ -count=1`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/decoder/decoder.go internal/decoder/decoder_test.go internal/mcpserver/tools_v10.go internal/mcpserver/tools_v10_test.go internal/mcpserver/server.go
git commit -m "feat(decoder,mcp): tool jwt_decode com warnings de ataque"
```

---

### Task 4: `jwt_resign` — resign offline com `confirmed:true`

**Files:**
- Modify: `internal/decoder/decoder.go`, `internal/decoder/decoder_test.go`
- Modify: `internal/mcpserver/tools_v10.go`, `tools_v10_test.go`, `server.go`

**Interfaces:**
- Consumes: `b64url` (Task 3), `crypto/hmac` + `crypto/sha256` (stdlib), `base64.RawURLEncoding`.
- Produces: `func ResignJWT(token, alg, secret string) (string, error)`. `alg "none"` → header re-encodado com `alg:none` + payload original, sem assinatura (termina com `.`); `"hs256"` → header `{"alg":"HS256","typ":"JWT"}`, sig = HMAC-SHA256(secret, `header.payload`) em base64url.

- [ ] **Step 1: Write the failing test**

```go
// decoder_test.go — adicionar
func TestResignJWT_None(t *testing.T) {
	tok := "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1MSJ9.c2ln"
	out, err := ResignJWT(tok, "none", "")
	if err != nil {
		t.Fatal(err)
	}
	parts := strings.Split(out, ".")
	if len(parts) != 3 || parts[2] != "" {
		t.Errorf("out = %q, want 3 partes com sig vazia", out)
	}
	hdr, _ := b64url(parts[0])
	if !strings.Contains(hdr, `"none"`) {
		t.Errorf("header = %s, want alg none", hdr)
	}
}

func TestResignJWT_HS256(t *testing.T) {
	tok := "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1MSJ9.c2ln"
	out, err := ResignJWT(tok, "hs256", "s3cr3t")
	if err != nil {
		t.Fatal(err)
	}
	parts := strings.Split(out, ".")
	if len(parts) != 3 || parts[2] == "" {
		t.Fatalf("out = %q, want 3 partes assinadas", out)
	}
	mac := hmac.New(sha256.New, []byte("s3cr3t"))
	mac.Write([]byte(parts[0] + "." + parts[1]))
	if want := base64.RawURLEncoding.EncodeToString(mac.Sum(nil)); parts[2] != want {
		t.Errorf("sig = %s, want %s", parts[2], want)
	}
	if _, err := ResignJWT(tok, "rs256", "x"); err == nil {
		t.Error("esperava erro para alg nao suportado")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/decoder/ -run TestResignJWT -v`
Expected: FAIL (undefined: ResignJWT)

- [ ] **Step 3: Write minimal implementation**

```go
// decoder.go — adicionar
func ResignJWT(token, alg, secret string) (string, error) {
	parts := strings.Split(strings.TrimSpace(token), ".")
	if len(parts) < 2 {
		return "", fmt.Errorf("jwt invalido")
	}
	switch strings.ToLower(alg) {
	case "none":
		hdr := base64.RawURLEncoding.EncodeToString([]byte(`{"alg":"none","typ":"JWT"}`))
		return hdr + "." + parts[1] + ".", nil
	case "hs256":
		hdr := base64.RawURLEncoding.EncodeToString([]byte(`{"alg":"HS256","typ":"JWT"}`))
		signing := hdr + "." + parts[1]
		mac := hmac.New(sha256.New, []byte(secret))
		mac.Write([]byte(signing))
		return signing + "." + base64.RawURLEncoding.EncodeToString(mac.Sum(nil)), nil
	default:
		return "", fmt.Errorf("alg %q nao suportado: use none ou hs256", alg)
	}
}
```

Handler `toolJwtResign`: args `token` (required), `alg` (required, Enum none/hs256), `secret`, `confirmed` (boolean Required). Se `confirmed != true` → erro `"confirmed=true obrigatorio"` (padrão add_target). Retorno: string do novo token. Registro + description: "Re-sign a JWT offline for acceptance testing (alg=none drops the signature; hs256 signs with the supplied secret). No traffic is sent to any target. Requires confirmed=true."

Teste MCP: (a) sem confirmed → erro; (b) com confirmed → token retornado.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/decoder/ ./internal/mcpserver/ -count=1`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/decoder/decoder.go internal/decoder/decoder_test.go internal/mcpserver/tools_v10.go internal/mcpserver/tools_v10_test.go internal/mcpserver/server.go
git commit -m "feat(decoder,mcp): tool jwt_resign offline com confirmed=true"
```

---

### Task 5: Briefing rico no `get_active_context`

**Files:**
- Modify: `internal/mcpserver/tools_v2.go` (`toolGetActiveContext`), `internal/mcpserver/tools_v2_test.go`
- Modify: `internal/mcpserver/server.go:110-113` (description)

**Interfaces:**
- Consumes: `st.List(ListFilter{Limit: 100000})`, `st.ListEndpoints()`, `s.active.Target()`, `s.active.Project()` — tudo existente.
- Produces: retorno JSON de `toolGetActiveContext` com campos novos: `status_counts` (map[int]int), `top_hosts` (máx 5, []struct{Host string; Count int}), `endpoints` (int), `scope_defined` (bool: len(tgt.InScopePatterns) > 0).

- [ ] **Step 1: Write the failing test**

```go
// tools_v2_test.go — adicionar
func TestToolGetActiveContext_Briefing(t *testing.T) {
	s, cleanup := newTestServer(t)
	defer cleanup()
	st, _ := s.openStoreForActiveTarget()
	for i := 0; i < 3; i++ {
		req := &store.Request{Method: "GET", URL: "https://a.com/x", Status: 200}
		if i == 2 {
			req.URL = "https://b.com/y"
			req.Status = 500
		}
		if _, err := st.Insert(req); err != nil {
			t.Fatal(err)
		}
	}
	out, err := s.toolGetActiveContext(context.Background(), map[string]any{})
	if err != nil {
		t.Fatal(err)
	}
	for _, want := range []string{`"status_counts"`, `"200":2`, `"500":1`, `"top_hosts"`, `"a.com"`, `"endpoints"`, `"scope_defined"`} {
		if !strings.Contains(out, want) {
			t.Errorf("briefing missing %q:\n%s", want, out)
		}
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/mcpserver/ -run TestToolGetActiveContext -v`
Expected: FAIL (campos novos ausentes)

- [ ] **Step 3: Implement**

Em `toolGetActiveContext`: manter campos atuais (projeto/alvo/contagem). Se houver alvo ativo e store aberto, agregar:

```go
	sums, err := st.List(store.ListFilter{Limit: 100000}) // ponytail: scan em memoria, SQL GROUP BY se >50k requests
	if err == nil {
		statusCounts := map[int]int{}
		hostCounts := map[string]int{}
		for _, r := range sums {
			statusCounts[r.Status]++
			if u, e := url.Parse(r.URL); e == nil && u.Host != "" {
				hostCounts[u.Host]++
			}
		}
		// ordenar top 5 hosts por count desc
	}
	eps, _ := st.ListEndpoints()
	scopeDefined := len(tgt.InScopePatterns) > 0
```

Retornar `json.Marshal` do struct com todos os campos (antigos + novos). Description em `server.go:111`: "Session briefing: active project/target, request count, status-code histogram, top 5 hosts, endpoint count, scope status. Call this FIRST at session start (<500 tokens)."

- [ ] **Step 4: Run tests**

Run: `go test ./internal/mcpserver/ -count=1`
Expected: PASS (inclui testes antigos de get_active_context — adaptar asserts se necessário)

- [ ] **Step 5: Commit**

```bash
git add internal/mcpserver/tools_v2.go internal/mcpserver/tools_v2_test.go internal/mcpserver/server.go
git commit -m "feat(mcp): briefing rico no get_active_context"
```

---

### Task 6: Smoke test e2e do binário no CI

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: comportamento de `cmd/mcp-proxy/main.go:52-54` (sem args → MCP stdio server).

- [ ] **Step 1: Adicionar step após Build**

```yaml
      - name: Smoke (MCP stdio e2e)
        run: |
          CGO_ENABLED=0 go build -trimpath -o ./mcp-proxy ./cmd/mcp-proxy
          {
            printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}'
            printf '%s\n' '{"jsonrpc":"2.0","method":"notifications/initialized"}'
            printf '%s\n' '{"jsonrpc":"2.0","id":2,"method":"tools/list"}'
          } | timeout 30 ./mcp-proxy > smoke_out.jsonl
          grep -q '"export_curl"' smoke_out.jsonl
          grep -q '"jwt_resign"' smoke_out.jsonl
          grep -q '"get_active_context"' smoke_out.jsonl
```

(Conferir a protocolVersion que o mcp-go v0.57 anuncia — o teste aceita qualquer resposta de tools/list com os nomes; se o initialize falhar por versão, usar a versão que o SDK aceita.)

- [ ] **Step 2: Verificar localmente**

Run: `CGO_ENABLED=0 go build -trimpath -o /tmp/mcp-proxy ./cmd/mcp-proxy && { printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}'; printf '%s\n' '{"jsonrpc":"2.0","method":"notifications/initialized"}'; printf '%s\n' '{"jsonrpc":"2.0","id":2,"method":"tools/list"}'; } | timeout 30 /tmp/mcp-proxy | grep -c export_curl`
Expected: >= 1

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "build(ci): smoke test e2e do binario (initialize + tools/list)"
```

---

### Task 7: Docs — README + MELHORIAS + COMMANDS

**Files:**
- Modify: `README.md:270-284` (roadmap), `README.md` (tabela de tools, contagem "34" → 48)
- Modify: `MELHORIAS.md` (checklist ✅ nos blocos entregues: P0, P1 §2.1–2.4, §2.5 via briefing, P2 §3 (M&R/scanner), §3.1 CTF tools, §5 ergonomia parcial)
- Modify: `docs/COMMANDS.md` (seção nova com as 5 tools)

- [ ] **Step 1: README roadmap**

```markdown
- ✅ **v5**: Decoder (`decode`/`encode`), comparer, extensions API, reports (markdown/html/pdf), tags/comments — 43 MCP tools total.
- ✅ **v5.1**: CTF tools (`export_curl`, `export_har`, `jwt_decode`, `jwt_resign`) + rich session briefing (`get_active_context`) + binary smoke test in CI — 48 MCP tools total.
```

Substituir o bloco "Coming next (see PRD for full list): 🔜 v5 — Decoder, comparer, extensions API (Go plugins)." por "All roadmap phases delivered. See MELHORIAS.md for future ideas."

- [ ] **Step 2: README tabela** — adicionar 5 linhas:

```markdown
| `export_curl` | Rebuild a captured request as a ready-to-paste curl command |
| `export_har` | Export the active target as HAR 1.2 (metadata only) |
| `jwt_decode` | Decode a JWT with attack-surface warnings (alg=none, empty sig, expired) |
| `jwt_resign` | Re-sign a JWT offline (none/HS256) — requires `confirmed: true` |
| `get_active_context` | Session briefing: status histogram, top hosts, endpoints, scope |
```

(Atualizar a linha existente de `get_active_context` em vez de duplicar.)

- [ ] **Step 3: MELHORIAS.md** — no fim de cada bloco entregue adicionar:

```markdown
> ✅ Entregue — ver roadmap v5.1 no README.
```

- [ ] **Step 4: COMMANDS.md** — seção `## v5.1 — CTF & briefing` com as 5 tools (nome, args, exemplo de pergunta ao assistente).

- [ ] **Step 5: Verificação final + commit**

Run: `go vet ./... && go test -race -count=1 ./... && grep -c 'mcp.NewTool("' internal/mcpserver/server.go`
Expected: tudo verde; contagem de tools únicas = 48.

```bash
git add README.md MELHORIAS.md docs/COMMANDS.md
git commit -m "docs: roadmap v5.1 + checklist MELHORIAS + COMMANDS"
```
